---
id: RE-003
slug: user_testing_checkpoint_non_editor_modes_ready
name: "Spec Revision - User testing checkpoint \u2014 non-editor modes ready"
created: '2026-03-04'
updated: '2026-03-04'
status: draft
kind: revision
aliases: []
relations: []
source_specs:
- PROD-001
requirements:
- PROD-001.FR-008
- PROD-001.FR-010
- PROD-001.FR-012
- PROD-001.FR-013
- PROD-001.FR-014
- PROD-001.FR-015
- PROD-001.FR-016
- PROD-001.FR-017
- PROD-001.NF-002
---

## 1. Context
- **Why**: DE-001 and DE-002 are complete. User testing confirmed core write/read
  path works well (inline, pipe, `-t`, `-r`, timestamps). Performance is ~1ms per
  invocation — NF-002 satisfied informally. Testing surfaced four new feature
  requests and one enhancement to existing behaviour.
- **Source Specs**: PROD-001
- **Requirements — implemented (active)**: FR-001–FR-007, FR-009, FR-011,
  FR-012, NF-001, NF-003
- **Requirements — deferred (pending)**: FR-008 (editor mode), FR-010
  (editor timestamp config), NF-002 (formal benchmark — informally satisfied)

## 2. Related Artefacts
- **Deltas**: DE-001 (completed), DE-002 (completed)
- **Revisions**: RE-001 (DE-001 completion), RE-002 (DE-002 completion)
- **Follow-up Delta**: DE-003 (to be scoped after this revision is accepted)

## 3. User Feedback — New Requirements

### FR-013: Print daily file path (`-f` flag)
When `-f` flag is provided, system MUST print the absolute path to today's
log file to stdout and exit. No entry is written.

**Rationale**: Enables shell composition (`vim $(im -f)`, `cat $(im -f) | rg ...`).

### FR-014: Configurable file template
The daily file format MUST be defined by an editable Markdown/Jinja template
(default location: `~/.config/im/template.md` or bundled default). The template
is rendered on first file creation each day.

**Rationale**: Users want control over file structure — frontmatter, headings,
metadata fields.

### FR-015: Template dynamic variables
The template engine MUST support the following variables:
- `created_at` — file creation timestamp, format `YYYY-MM-DD HH:MM`
- `updated_at` — last entry timestamp, format `YYYY-MM-DD HH:MM`
- `id` — 7-character nanoID, generated once per file on creation
- `title` — rendered from a configurable format string in `im.toml`
  (e.g. `title_format = "Log — {{ date }}"`)

`updated_at` MUST be refreshed on each entry append (frontmatter patch).

### FR-012 (revised): Read mode split — `-r` renders, `-p` pages
`-r` now renders to stdout via `glow` (if available) or `cat` — no pager.
The previous pager behaviour moves to a new `-p` flag (FR-017).

**Rationale**: Quick read (`-r`) is the common case; paged view (`-p`) is
for longer review sessions. Keeps `-r` fast and composable with pipes.

### FR-017: Pager mode (`-p` flag)
When `-p` flag is provided, system MUST display today's log file in a pager
using the first available viewer: `glow --pager`, `rich --markdown --pager`,
`$PAGER`, or `cat`. This is the previous `-r` behaviour.

### FR-016: 12-hour timestamp format
Timestamp subheadings MUST support 12-hour format with AM/PM, configurable
via `im.toml` key `time_format` with values:
- `"24h"` (default) — `## 14:30`
- `"12h"` — `## 2:30 PM`

## 4. Existing Deferred Requirements
- **FR-008**: Editor mode — still deferred, independent of above
- **FR-010**: Editor timestamp config — depends on FR-008
- **NF-002**: Formal benchmark — informally satisfied at ~1ms, formal
  `testing.B` still deferred

## 5. Actions
- [ ] Patch FR-013–FR-016 into PROD-001
- [ ] Update PROD-001 capabilities block for new requirements
- [ ] Scope DE-003 to implement FR-013–FR-016 (and optionally FR-008/FR-010)
- [ ] Accept this revision

## 6. Decisions
- **Template engine**: Go `text/template` — native, no external dep.
- **nanoID**: `github.com/matoous/go-nanoid/v2`, 7-char default.
- **`updated_at` mutation**: Only patched when the field is already present
  in the file's frontmatter. If the template omits `updated_at`, or the
  field is missing from the file, the append path does not touch prior
  content. This preserves FR-002 append-only semantics for files that
  don't opt into frontmatter timestamps.
