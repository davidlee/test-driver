---
id: IP-001
name: Implementation Plan - Initial Go CLI scaffold and architecture
slug: ip_001_go_cli_scaffold
kind: plan
status: draft
created: '2026-03-04'
updated: '2026-03-04'
relations:
- type: implements
  target: DE-001
objective: >-
  Bootstrap the im binary: Go module, urfave/cli app, config loading,
  lint/test infrastructure. Produces a compilable, installable binary
  that parses flags and detects input mode but does not yet write entries.
entrance_criteria:
  - DE-001 and DR-001 accepted
  - PROD-001 requirements synced
exit_criteria:
  - im binary builds and installs to ~/.local/bin
  - im --help prints usage
  - Config loads defaults with no im.toml present
  - Config reads overrides from im.toml when present
  - just check exits 0 (lint + tests green)
---

# IP-001 – Initial Go CLI scaffold and architecture

```yaml supekku:plan.overview@v1
schema: supekku.plan.overview
version: 1
plan: IP-001
delta: DE-001
specs:
  primary:
    - PROD-001
  collaborators: []
requirements:
  targets:
    - PROD-001.NF-001
    - PROD-001.NF-003
  dependencies: []
phases:
  - id: IP-001.PHASE-01
    name: Phase 1 - Go module and build skeleton
    objective: go mod init, entrypoint, justfile recipes, golangci-lint passing
    entrance_criteria:
      - DR-001 reviewed
    exit_criteria:
      - go build ./cmd/im succeeds
      - just check exits 0
      - im --help prints stub usage

  - id: IP-001.PHASE-02
    name: Phase 2 - Config loading
    objective: TOML config with defaults, env fallback, path expansion
    entrance_criteria:
      - Phase 1 complete
    exit_criteria:
      - Config struct populated from defaults when no file exists
      - Config struct populated from im.toml when present
      - All config keys tested (log_dir, editor, editor_timestamp, timestamp_rounding)
      - just check exits 0

  - id: IP-001.PHASE-03
    name: Phase 3 - CLI flags and input mode detection
    objective: Wire urfave/cli flags (-t, -r), positional args, --, stdin/TTY detection
    entrance_criteria:
      - Phase 2 complete (config available to CLI)
    exit_criteria:
      - -t and -r flags parsed correctly
      - Input mode detection returns inline/pipe/editor for correct conditions
      - -- separator works
      - just check exits 0
```

## 1. Summary
- **Delta**: DE-001 – Initial Go CLI scaffold and architecture
- **Specs Impacted**: PROD-001
- **Desired Outcome**: A compilable, installable `im` binary with config
  loading, CLI parsing, and input mode detection — ready for entry/logfile
  logic in subsequent deltas.

## 2. Context & Constraints
- **Current Behaviour**: No code exists.
- **Target Behaviour**: NF-001 (zero-config), NF-003 (single binary).
- **Dependencies**: None (first delta).
- **Constraints**: urfave/cli as sole framework dep. golangci-lint must pass
  from the first commit (POL-001).

## 3. Gate Check
- [x] PROD-001 requirements synced
- [x] DR-001 design revision drafted
- [x] POL-001 zero-tolerance policy in place
- [ ] Test strategy: unit tests per package, table-driven

## 4. Phase Overview
| Phase | Objective | Exit Criteria | Phase Sheet |
|---|---|---|---|
| 1 — Module & build | go.mod, entrypoint, justfile, lint green | `just check` exits 0, `im --help` works | `phases/phase-01.md` |
| 2 — Config | TOML loading, defaults, path expansion | Config tests pass, `just check` exits 0 | `phases/phase-02.md` |
| 3 — CLI & input modes | Flags, args, stdin/TTY detection | All input modes detected correctly, `just check` exits 0 | `phases/phase-03.md` |

## 5. Testing & Verification Plan
- **Unit tests**: Table-driven tests for config loading (defaults, file,
  invalid), CLI flag parsing, input mode detection.
- **Integration**: `go build` + run binary with various arg combos.
- **Tooling**: `just check` = `just lint` + `just test`.

## 6. Risks & Mitigations
| Risk | Mitigation |
|---|---|
| urfave/cli v3 API instability | Pin to latest stable v2 or v3 release |
| golangci-lint false positives on fresh scaffold | Tune exclusions in .golangci.yml if needed |

## 7. Progress Tracking
- [x] Phase 1 complete
- [x] Phase 2 complete
- [x] Phase 3 complete
- [x] All verification gates passed
