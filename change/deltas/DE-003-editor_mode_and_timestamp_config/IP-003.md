---
id: IP-003
name: Implementation Plan - Editor mode and timestamp config
slug: ip_003_editor_mode_and_timestamp_config
kind: plan
status: draft
created: '2026-03-04'
updated: '2026-03-04'
relations:
- type: implements
  target: DE-003
objective: >-
  Add editor input mode and editor_timestamp config to im. After this plan,
  all input modes from PROD-001 are functional.
entrance_criteria:
  - DE-003 and DR-003 accepted
  - DE-002 complete (merged)
exit_criteria:
  - im (no args, TTY) opens editor, saved content appends to daily file
  - Empty/aborted edit produces no file mutation
  - editor_timestamp start/end config respected
  - VH-001–VH-005 manual editor verifications passed
  - just check exits 0
---

# IP-003 – Editor mode and timestamp config

```yaml supekku:plan.overview@v1
schema: supekku.plan.overview
version: 1
plan: IP-003
delta: DE-003
specs:
  primary:
  - PROD-001
  collaborators: []
requirements:
  targets:
  - PROD-001.FR-008
  - PROD-001.FR-010
  dependencies:
  - DE-002
phases:
- id: IP-003.PHASE-01
  name: Phase 1 - Editor package
  objective: Editor detection, launch, save logic, content validation
  entrance_criteria:
  - DR-003 reviewed
  exit_criteria:
  - "detectEditor returns first available from config \u2192 $EDITOR \u2192 defaults"
  - Edit returns content on exit 0, empty on non-zero/abort
  - Content validation rejects non-UTF-8, oversized, binary
  - Table-driven tests with injected CommandChecker
  - just check exits 0
- id: IP-003.PHASE-02
  name: Phase 2 - Wiring and timestamp config
  objective: Wire editor into cmd/im, implement editor_timestamp start/end
  entrance_criteria:
  - Phase 1 complete (editor package available)
  exit_criteria:
  - im (no args, TTY) opens detected editor
  - Saved content appends to daily file under correct timestamp
  - Empty save exits cleanly, no file mutation
  - editor_timestamp=start uses pre-editor time (default)
  - editor_timestamp=end uses post-editor time
  - just check exits 0
- id: IP-003.PHASE-03
  name: Phase 3 - Manual editor verification
  objective: "VH-001\u2013VH-005: verify each editor in FR-008 list"
  entrance_criteria:
  - Phase 2 complete (editor mode wired)
  exit_criteria:
  - VH-001 neovim verified
  - VH-002 emacs verified
  - VH-003 helix verified
  - VH-004 zed verified
  - VH-005 vscode verified
```

## 1. Summary
- **Delta**: DE-003 – Editor mode and timestamp config
- **Specs Impacted**: PROD-001
- **Desired Outcome**: All PROD-001 input modes functional. Editor opens
  on `im` with no args + TTY, `editor_timestamp` config controls which
  time is used.

## 2. Context & Constraints
- **Current Behaviour**: Editor mode prints stub message and exits.
- **Target Behaviour**: FR-008 (editor mode), FR-010 (editor timestamp).
- **Dependencies**: DE-002 complete (merged). New dep: `github.com/confluentinc/go-editor`.
- **Constraints**: Must not regress inline path. Detection chain must handle
  editors not installed gracefully.

## 3. Gate Check
- [x] DE-002 complete
- [x] DR-003 design revision drafted
- [ ] Test strategy: TDD, table-driven unit tests, mock editor script for
  integration, VH-001–VH-005 for manual verification

## 4. Phase Overview
| Phase | Objective | Exit Criteria | Phase Sheet |
|---|---|---|---|
| 1 — Editor package | Detection, launch, save, validation | Tests green, `just check` 0 | `phases/phase-01.md` |
| 2 — Wiring + timestamp | Wire cmd/im, editor_timestamp config | Editor mode works end-to-end | `phases/phase-02.md` |
| 3 — Manual verification | VH-001–VH-005 per-editor checks | All 5 editors verified | `phases/phase-03.md` |

## 5. Testing & Verification Plan
- **Unit tests**: Editor detection (injected checker), save logic, content
  validation (UTF-8, size, binary). Table-driven.
- **Integration**: Mock editor script (writes known content to temp file),
  verify entry appears in daily file.
- **Manual (VH)**: Each editor in FR-008 list — open, type, save, verify
  entry appended. Test abort (`:q!` / ^C) produces no mutation.
- **Tooling**: `just check` = `just lint` + `just test`.

## 6. Risks & Mitigations
| Risk | Mitigation |
|---|---|
| Editor flags vary (`--wait` for vscode/zed) | Detection chain includes flags; document in config |
| go-editor lib temp file cleanup | Defer remove in launch function; test cleanup |
| TTY detection in CI | Existing `cli.IsTerminal` var already stubbed |

## 7. Progress Tracking
- [ ] Phase 1 complete
- [ ] Phase 2 complete
- [ ] Phase 3 complete
- [ ] Verification gates passed
