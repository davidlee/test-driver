# Execution Environment

Run spec-driver via: `spec-driver`

Any instruction like `spec-driver $args` means `spec-driver $args`.
For example: `spec-driver list adrs` → `spec-driver list adrs`.

Run `spec-driver --help` for available commands.

Verification: `just check`
