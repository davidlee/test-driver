# Hooks

User-customizable files installed to `.spec-driver/hooks/`.

These are seeded on first install and never overwritten, so local edits are safe.

## Files

- `doctrine.md` — Project-specific conventions and preferences loaded by agent skills (boot, scope-delta, shape-revision, doctrine). Keep it concise; it's read early in every agent session.
