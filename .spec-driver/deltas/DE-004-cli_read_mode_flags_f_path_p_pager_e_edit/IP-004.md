---
id: IP-004
slug: cli_read_mode_flags_f_path_p_pager_e_edit
name: 'Implementation Plan - CLI read-mode flags: -f path, -p pager, -e edit'
created: '2026-03-05'
updated: '2026-03-08'
status: draft
kind: plan
aliases: []
---

```yaml supekku:plan.overview@v1
schema: supekku.plan.overview
version: 1
plan: IP-004
delta: DE-004
revision_links:
  aligns_with:
  - RE-006
specs:
  primary:
  - PROD-001
  collaborators: []
requirements:
  targets:
  - PROD-001.FR-013
  - PROD-001.FR-017
  - PROD-001.FR-019
  dependencies: []
phases:
- id: IP-004-P01
- id: IP-004.PHASE-01
```

```yaml supekku:verification.coverage@v1
schema: supekku.verification.coverage
version: 1
subject: IP-004
entries:
  - artefact: VT-NEW
    kind: VT
    requirement: PROD-001.FR-013
    status: planned
    notes: "-f prints correct absolute path, exits 0, no file mutation"
  - artefact: VT-NEW
    kind: VT
    requirement: PROD-001.FR-017
    status: planned
    notes: "-p dispatches to pager viewer (existing ResolveViewer)"
  - artefact: VT-NEW
    kind: VT
    requirement: PROD-001.FR-019
    status: planned
    notes: "-e opens file in editor; message when no file exists"
  - artefact: VT-NEW
    kind: VT
    requirement: PROD-001.FR-012
    status: planned
    notes: "Render dispatches to stdout viewer (no --pager); -r fix"
  - artefact: VT-010
    kind: VT
    requirement: PROD-001.FR-012
    status: regression
    notes: "-r behavior changes from pager to stdout — update existing test"
```

## 1. Summary
- **Delta**: DE-004 — CLI read-mode flags: -f path, -p pager, -e edit
- **Specs Impacted**: PROD-001
- **Desired Outcome**: `im -f`, `im -p`, `im -e` functional; `-r` corrected to stdout (no pager)

## 2. Context & Constraints
- **Current Behaviour**: Only `-r` exists, calls `reader.View` (pager mode via `glow --pager`). No `-f`, `-p`, `-e`.
- **Target Behaviour**: FR-013 (`-f`), FR-017 (`-p`), FR-019 (`-e`), FR-012 correction (`-r` → stdout)
- **Dependencies**: DE-003 (completed — editor detection available)
- **Constraints**: No new dependencies. Reuse `editor.detectEditor` for `-e`.

## 3. Gate Check
- [x] Backlog items linked and prioritised
- [x] Spec(s) updated (RE-006 adds FR-019)
- [x] Test strategy identified (unit tests per flag + reader dispatch)
- [x] Workspace/config changes assessed (none needed)

## 4. Phase Overview
| Phase | Objective | Entrance Criteria | Exit Criteria / Done When | Phase Sheet |
| --- | --- | --- | --- | --- |
| P01 — Implement all flags | Add -f, -p, -e flags; split reader Render/View; add editor.EditFile | DR-004 reviewed, code read | All flags work, `just check` green, tests cover each flag | `phases/phase-01.md` |

Single phase — all changes are small, share the same code surface, and have no inter-dependencies.

## 5. Phase Detail Snapshot
- **Design Revision**: `DE-004/DR-004.md`
- **Active Phase Sheet**: `phases/phase-01.md`

## 6. Testing & Verification Plan
- **Updated Suites**: `internal/reader/reader_test.go`, `cmd/im/main_test.go`
- **New Cases**:
  - `reader.ResolveRenderer` finds `cat` (stdout, no `--pager`)
  - `reader.Render` returns `ErrNoFile` for missing file
  - `runFile` prints absolute path
  - `runPager` delegates to `reader.View`
  - `runEdit` calls `editor.EditFile`; prints message when no file
  - `-r` now calls `Render` (not `View`)
- **New Functions**: `editor.EditFile`, `reader.Render`, `reader.ResolveRenderer`

## 7. Risks & Mitigations
| Risk | Mitigation | Owner |
| --- | --- | --- |
| Pager dispatch portability | Fallback chain ends at `cat` | Dev |
| `-e` vs editor mode confusion | `-e` is explicit flag, editor mode is implicit (no args + TTY) | Dev |
| `-r` regression | Update VT-010, verify existing tests | Dev |

## 8. Open Questions & Decisions
None — DR-004 resolved all questions.

## 9. Progress Tracking
- [ ] P01 complete
- [ ] Verification gates passed

## 10. Notes / Links
- DR-004 contains full code sketches for all changes
